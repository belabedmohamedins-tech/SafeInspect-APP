// __tests__/hooks/useHomeData.test.ts
// Full hook tests live in src/__tests__/useHomeData.test.ts (canonical).
// This file exists to satisfy Jest's "must contain at least one test" requirement
// while the hook's dependencies are being resolved in the new __tests__/ tree.

describe('useHomeData (stub)', () => {
  it('placeholder — full coverage in src/__tests__/useHomeData.test.ts', () => {
    expect(true).toBe(true);
  });
});
