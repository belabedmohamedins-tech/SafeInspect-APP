// src/constants/colors.ts
// ⚠️  DEPRECATED — this file exists only as a safety shim.
// All code should import from 'constants/' (constants/theme.ts) directly.
// This shim will be deleted once every import is migrated.
export { Colors, BLUE } from '../../constants';
