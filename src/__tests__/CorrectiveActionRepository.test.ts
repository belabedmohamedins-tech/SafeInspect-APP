// src/__tests__/CorrectiveActionRepository.test.ts
// STALE DUPLICATE — canonical suite is __tests__/repositories/CorrectiveActionRepository.test.ts
// This file caused flaky failures due to same-millisecond createdAt ordering.
// Replaced with a no-op to avoid duplicate execution while preserving the file.

describe('CorrectiveActionRepository (legacy stub)', () => {
  it('redirected — see __tests__/repositories/CorrectiveActionRepository.test.ts', () => {
    expect(true).toBe(true);
  });
});
