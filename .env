# Node 22 ships native TypeScript type-stripping which crashes on
# node_modules/.ts files (e.g. expo-modules-core). This flag disables it
# so Metro/Babel handle all transpilation as intended.
# Remove once Expo officially supports Node 22 (expected SDK 54+).
NODE_OPTIONS=--no-experimental-strip-types
